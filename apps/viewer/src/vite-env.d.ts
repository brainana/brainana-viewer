/// <reference types="vite/client" />

// Compile-time constant injected by Vite's `define` (see apps/viewer/vite.config.ts), sourced
// from the root package.json `version`. Used for the toolbar version line.
declare const __APP_VERSION__: string
