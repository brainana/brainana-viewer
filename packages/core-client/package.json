{
  "name": "@brainana/core-client",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Framework-agnostic browser platform layer shared by brainana tools: token-authed runtime client, source manager, filesystem client, session persistence, export destinations, browser capability gate.",
  "//exports": "Subpaths resolve to raw .ts (no build) so Vite + Node type-stripping resolve identically.",
  "exports": {
    "./*": "./*"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
